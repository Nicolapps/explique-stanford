kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name:  APPLICATION-NAME-pvc
  namespace: thl2
spec:
  volumeName: APPLICATION-NAME-pv
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
